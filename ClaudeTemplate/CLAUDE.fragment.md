## SCP_Core 共用規則

本專案以 `SCP_Core` 為 git submodule，跨專案的 agent 機制（指令系統 / 後台頁 / 記憶與信件層）
由它集中管理。下一行的 `@` 是 **Claude Code 專屬**的 inline 載入 ——
改 SCP_Core 端規則，下次 session 自動同步。

@{{SCP_CORE_PATH}}/AgentEntry/SCP_Core_Entry.md
